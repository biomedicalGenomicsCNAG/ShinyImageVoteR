# Initialize the database connection pool

Initialize the database connection pool

## Usage

``` r
init_db(cfg_sqlite_file)
```

## Arguments

- cfg_sqlite_file:

  Path to an existing SQLite file

## Value

A DBI pool object
