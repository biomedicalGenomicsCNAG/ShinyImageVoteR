# Populate the annotations table with data from a file

Populate the annotations table with data from a file

## Usage

``` r
populate_annotations_table(conn, to_be_voted_images_file)
```

## Arguments

- conn:

  Database connection object

- to_be_voted_images_file:

  Character. Path to the file containing image annotations
