# FAQ module server logic

FAQ module server logic

## Usage

``` r
faqServer(id, cfg, tab_trigger = NULL)
```

## Arguments

- id:

  A string identifier for the module namespace.

- cfg:

  App configuration

- tab_trigger:

  Optional reactive that triggers when the about tab is selected

## Value

None. Side effect only: registers a module server.
