% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_login.R
\name{loginServer}
\alias{loginServer}
\title{Login module server logic}
\usage{
loginServer(id, db_conn = NULL, log_out = reactive(NULL))
}
\arguments{
\item{id}{A string identifier for the module namespace.}

\item{db_conn}{A database pool connection (e.g. SQLite or PostgreSQL) used to track sessions.}

\item{log_out}{A reactive trigger (default: \code{reactive(NULL)}) to perform logout actions.}
}
\value{
A list containing:
\describe{
\item{login_data}{A \code{reactiveVal} holding login metadata (e.g. user ID, voting institute, session ID)}
\item{credentials}{A \code{reactive} object with user authentication status}
\item{update_logout_time}{A function to record logout time for a session ID}
}
}
\description{
Handles user authentication and session tracking using the \code{shinyauthr} package.
This module supports login via a database-backed user table and emits reactive
values for downstream modules to consume.
}
\details{
It also supports optional logout triggering and updates the session tracking database.
}
