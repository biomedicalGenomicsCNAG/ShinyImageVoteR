% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_env_utils.R
\name{safe_dir_create}
\alias{safe_dir_create}
\title{Create a Directory Safely}
\usage{
safe_dir_create(
  path,
  pattern = "^[A-Za-z0-9_]+$",
  showWarnings = TRUE,
  recursive = FALSE
)
}
\arguments{
\item{path}{Character. The name (or path) of the directory to create.
Only the final component (basename) is validated.}

\item{pattern}{Character. A regular expression that the directory name must match.
Default is \code{"^[A-Za-z0-9_]+$"}, i.e. one or more letters, digits, or underscores.}

\item{showWarnings}{Logical. If \code{TRUE}, warns when the directory already exists or cannot be created.
Default is \code{TRUE}.}

\item{recursive}{Logical. If \code{TRUE}, creates any missing parent directories (like \code{mkdir -p}). Default is \code{FALSE}.}

\item{mode}{Character or numeric. Directory permissions in octal (e.g. \code{"0777"}). Default is \code{"0777"}.}
}
\value{
Invisibly returns \code{TRUE} if the directory was successfully created, \code{FALSE} otherwise.
If the directory already exists, returns \code{FALSE} (with a message, unless \code{showWarnings = FALSE}).
}
\description{
Validates that the target directory name is a single “word” (letters, digits, and/or underscores only)
before attempting to create it. Optionally handles nested creation, warnings, and permission bits.
}
\examples{
# Successful creation
safe_dir_create("data_folder")

# Fails validation (contains spaces)
try(safe_dir_create("my data"))
}
